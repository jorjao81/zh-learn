/*
 * Speech Services API version 3.2
 * Speech Services API version 3.2.
 *
 * The version of the OpenAPI document: 3.2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.StringJoiner;
import java.util.Objects;
import java.util.Map;
import java.util.HashMap;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import org.openapitools.client.model.ResponseBlock;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;


import org.openapitools.client.ApiClient;
/**
 * List of uploaded blocks.
 */
@JsonPropertyOrder({
  UploadedBlocks.JSON_PROPERTY_COMMITTED_BLOCKS,
  UploadedBlocks.JSON_PROPERTY_UNCOMMITTED_BLOCKS
})
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2025-03-03T10:07:46.057250+01:00[Europe/Berlin]", comments = "Generator version: 7.12.0")
public class UploadedBlocks {
  public static final String JSON_PROPERTY_COMMITTED_BLOCKS = "committedBlocks";
  @javax.annotation.Nullable
  private List<ResponseBlock> committedBlocks = new ArrayList<>();

  public static final String JSON_PROPERTY_UNCOMMITTED_BLOCKS = "uncommittedBlocks";
  @javax.annotation.Nullable
  private List<ResponseBlock> uncommittedBlocks = new ArrayList<>();

  public UploadedBlocks() { 
  }

  public UploadedBlocks committedBlocks(@javax.annotation.Nullable List<ResponseBlock> committedBlocks) {
    this.committedBlocks = committedBlocks;
    return this;
  }

  public UploadedBlocks addCommittedBlocksItem(ResponseBlock committedBlocksItem) {
    if (this.committedBlocks == null) {
      this.committedBlocks = new ArrayList<>();
    }
    this.committedBlocks.add(committedBlocksItem);
    return this;
  }

  /**
   * The block description of blocks already committed.
   * @return committedBlocks
   */
  @javax.annotation.Nullable
  @JsonProperty(JSON_PROPERTY_COMMITTED_BLOCKS)
  @JsonInclude(value = JsonInclude.Include.USE_DEFAULTS)
  public List<ResponseBlock> getCommittedBlocks() {
    return committedBlocks;
  }


  @JsonProperty(JSON_PROPERTY_COMMITTED_BLOCKS)
  @JsonInclude(value = JsonInclude.Include.USE_DEFAULTS)
  public void setCommittedBlocks(@javax.annotation.Nullable List<ResponseBlock> committedBlocks) {
    this.committedBlocks = committedBlocks;
  }


  public UploadedBlocks uncommittedBlocks(@javax.annotation.Nullable List<ResponseBlock> uncommittedBlocks) {
    this.uncommittedBlocks = uncommittedBlocks;
    return this;
  }

  public UploadedBlocks addUncommittedBlocksItem(ResponseBlock uncommittedBlocksItem) {
    if (this.uncommittedBlocks == null) {
      this.uncommittedBlocks = new ArrayList<>();
    }
    this.uncommittedBlocks.add(uncommittedBlocksItem);
    return this;
  }

  /**
   * The block description of blocks not committed to the blob.
   * @return uncommittedBlocks
   */
  @javax.annotation.Nullable
  @JsonProperty(JSON_PROPERTY_UNCOMMITTED_BLOCKS)
  @JsonInclude(value = JsonInclude.Include.USE_DEFAULTS)
  public List<ResponseBlock> getUncommittedBlocks() {
    return uncommittedBlocks;
  }


  @JsonProperty(JSON_PROPERTY_UNCOMMITTED_BLOCKS)
  @JsonInclude(value = JsonInclude.Include.USE_DEFAULTS)
  public void setUncommittedBlocks(@javax.annotation.Nullable List<ResponseBlock> uncommittedBlocks) {
    this.uncommittedBlocks = uncommittedBlocks;
  }


  /**
   * Return true if this UploadedBlocks object is equal to o.
   */
  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    UploadedBlocks uploadedBlocks = (UploadedBlocks) o;
    return Objects.equals(this.committedBlocks, uploadedBlocks.committedBlocks) &&
        Objects.equals(this.uncommittedBlocks, uploadedBlocks.uncommittedBlocks);
  }

  @Override
  public int hashCode() {
    return Objects.hash(committedBlocks, uncommittedBlocks);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class UploadedBlocks {\n");
    sb.append("    committedBlocks: ").append(toIndentedString(committedBlocks)).append("\n");
    sb.append("    uncommittedBlocks: ").append(toIndentedString(uncommittedBlocks)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }

  /**
   * Convert the instance into URL query string.
   *
   * @return URL query string
   */
  public String toUrlQueryString() {
    return toUrlQueryString(null);
  }

  /**
   * Convert the instance into URL query string.
   *
   * @param prefix prefix of the query string
   * @return URL query string
   */
  public String toUrlQueryString(String prefix) {
    String suffix = "";
    String containerSuffix = "";
    String containerPrefix = "";
    if (prefix == null) {
      // style=form, explode=true, e.g. /pet?name=cat&type=manx
      prefix = "";
    } else {
      // deepObject style e.g. /pet?id[name]=cat&id[type]=manx
      prefix = prefix + "[";
      suffix = "]";
      containerSuffix = "]";
      containerPrefix = "[";
    }

    StringJoiner joiner = new StringJoiner("&");

    // add `committedBlocks` to the URL query string
    if (getCommittedBlocks() != null) {
      for (int i = 0; i < getCommittedBlocks().size(); i++) {
        if (getCommittedBlocks().get(i) != null) {
          joiner.add(getCommittedBlocks().get(i).toUrlQueryString(String.format("%scommittedBlocks%s%s", prefix, suffix,
          "".equals(suffix) ? "" : String.format("%s%d%s", containerPrefix, i, containerSuffix))));
        }
      }
    }

    // add `uncommittedBlocks` to the URL query string
    if (getUncommittedBlocks() != null) {
      for (int i = 0; i < getUncommittedBlocks().size(); i++) {
        if (getUncommittedBlocks().get(i) != null) {
          joiner.add(getUncommittedBlocks().get(i).toUrlQueryString(String.format("%suncommittedBlocks%s%s", prefix, suffix,
          "".equals(suffix) ? "" : String.format("%s%d%s", containerPrefix, i, containerSuffix))));
        }
      }
    }

    return joiner.toString();
  }

    public static class Builder {

    private UploadedBlocks instance;

    public Builder() {
      this(new UploadedBlocks());
    }

    protected Builder(UploadedBlocks instance) {
      this.instance = instance;
    }

    public UploadedBlocks.Builder committedBlocks(List<ResponseBlock> committedBlocks) {
      this.instance.committedBlocks = committedBlocks;
      return this;
    }
    public UploadedBlocks.Builder uncommittedBlocks(List<ResponseBlock> uncommittedBlocks) {
      this.instance.uncommittedBlocks = uncommittedBlocks;
      return this;
    }


    /**
    * returns a built UploadedBlocks instance.
    *
    * The builder is not reusable.
    */
    public UploadedBlocks build() {
      try {
        return this.instance;
      } finally {
        // ensure that this.instance is not reused
        this.instance = null;
      }
    }

    @Override
    public String toString() {
      return getClass() + "=(" + instance + ")";
    }
  }

  /**
  * Create a builder with no initialized field.
  */
  public static UploadedBlocks.Builder builder() {
    return new UploadedBlocks.Builder();
  }

  /**
  * Create a builder with a shallow copy of this instance.
  */
  public UploadedBlocks.Builder toBuilder() {
    return new UploadedBlocks.Builder()
      .committedBlocks(getCommittedBlocks())
      .uncommittedBlocks(getUncommittedBlocks());
  }

}

